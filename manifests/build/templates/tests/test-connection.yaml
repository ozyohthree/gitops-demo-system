apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "build.fullname" . }}-test-connection"
  labels:
    {{- include "build.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "build.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
